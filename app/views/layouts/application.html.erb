<!DOCTYPE html>
<html>
  <head>
    <title>MTG-DB</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/css?family=Oswald:600|Roboto|Raleway:600|Merriweather:700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="navigation">
      <h1
        id="title"
        onClick="window.location.href = '/'"
      >
        MTG-DB
      </h1>

      <div id="navigation-menu">
        <button
          class="navigation-button <%= @params['controller'] == 'about' ? 'selected' : nil %>"
          onClick="window.location.href = '/about'"
        >
          About
        </button>
        <button
          class="navigation-button <%= @params['controller'] == 'cards' && @params['action'] == 'index' ? 'selected' : nil %>"
          onClick="window.location.href = '/cards'"
        >
          Cards
        </button>
        <button
          class="navigation-button <%= @params['controller'] == 'expansions' && @params['action'] == 'index' ? 'selected' : nil %>"
          onClick="window.location.href = '/expansions'"
        >
          Expansions
        </button>
      </div>
    </div>

    <div id="content-container">
      <%= yield %>
    </div>
  </body>
</html>
